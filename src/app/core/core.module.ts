import { NgModule } from '@angular/core';

/**
 * Represents a module that includes the application-wide and singleton
 * services, interceptors and guards.
 */
@NgModule({
  exports: []
})
export class CoreModule { }
